import STitle from "./styled/STitle";

const Logo = () => {
  return (
    <div>
      <STitle data-testid="title" fontSize="25px" fontWeight="700" rFontSize="16px" > 
        Free Games Finder 
      </STitle>
    </div>
  );
}

export default Logo;
